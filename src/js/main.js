/**
 * smoking
 */

/**
 *  need new config... it's too old now...
 *
 * declined gulp due different versions or smth( node-sass dislikes node v10 ? )
 *
 * add webpack...
 */

import jQuery from 'jquery';
import popper from 'popper.js';
import bootstrap from 'bootstrap';

jQuery(function () {
    jQuery('body').css('color', 'blue');
});